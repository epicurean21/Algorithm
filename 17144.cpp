/**
 * 17144 미세먼지 안녕!
 * 구현, 시뮬레이션
 */
#include <iostream>

using namespace std;
#define MAX 51

int R, C, T, A, map[MAX][MAX];
pair<pair<int,int>
void spread

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    cin >> R >> C >> T;
    for (int i = 0; i < R; i++)
        for (int j = 0; j < C; j++) {
            cin >> map[i][j];
            if (map[i][j] == -1)
        }
    while (T--) {
        // 미세먼지 확산
    }

    return 0;
}