#include <iostream>
#include <vector>

using namespace std;
int T, N, M;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    cin >> T;
    while (T--) {
        cin >> N >> M;
        vector<int> DVD(N + 1);
        for (int i = 0; i < N; i++)
            DVD[i] = i + 1;
        for (int j = 0; j < M; j++) {

        }
    }
    return 0;
}